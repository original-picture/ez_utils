# ez_utils
library of utilities that I wish were in the STL

## build
just add the project as a git submodule, then do `add_subdirectory` in cmake, and finally call `target_link_libraries` to link `ez_utils`